# Missing Tables Implementation Guide

**Status**: ⚠️ Original diagram is too complex to update in draw.io  
**Solution**: Use this guide to manually add tables or use the SQL scripts

---

## Quick Reference: What Tables to Add

### VIEWER SERVICE - 12 Missing Tables

**Copy these SQL definitions to your schema.sql**:

```sql
-- 1. RelatedParties
CREATE TABLE IF NOT EXISTS RelatedParties (
    RelatedPartyID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ClientID BIGINT,
    RelationType VARCHAR(50),
    TitlePrefix VARCHAR(20),
    FirstName VARCHAR(100) NOT NULL,
    LastName VARCHAR(100),
    DateOfBirth DATE,
    Status VARCHAR(50),
    FOREIGN KEY (ClientID) REFERENCES Clients(ClientID)
);

-- 2. RelatedPartyAddresses
CREATE TABLE IF NOT EXISTS RelatedPartyAddresses (
    AddressID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RelatedPartyID BIGINT,
    AddressType VARCHAR(50),
    AddressLine1 VARCHAR(255),
    City VARCHAR(100),
    Zip VARCHAR(20),
    Country VARCHAR(100),
    FOREIGN KEY (RelatedPartyID) REFERENCES RelatedParties(RelatedPartyID)
);

-- 3. RelatedPartyIdentifiers
CREATE TABLE IF NOT EXISTS RelatedPartyIdentifiers (
    IdentifierID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RelatedPartyID BIGINT,
    IdentifierType VARCHAR(50),
    IdentifierValue VARCHAR(100),
    IssuingAuthority VARCHAR(100),
    FOREIGN KEY (RelatedPartyID) REFERENCES RelatedParties(RelatedPartyID)
);

-- 4. CaseDocuments
CREATE TABLE IF NOT EXISTS CaseDocuments (
    DocumentID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CaseID BIGINT NOT NULL,
    DocumentName VARCHAR(255) NOT NULL,
    Category VARCHAR(100),
    MimeType VARCHAR(100),
    UploadedBy VARCHAR(50),
    Comment TEXT,
    Data BLOB,
    UploadDate TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (CaseID) REFERENCES Cases(CaseID)
);

-- 5. CaseEvents
CREATE TABLE IF NOT EXISTS CaseEvents (
    EventID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CaseID BIGINT NOT NULL,
    EventType VARCHAR(50),
    EventDescription TEXT,
    EventDate TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    EventSource VARCHAR(50),
    FOREIGN KEY (CaseID) REFERENCES Cases(CaseID)
);

-- 6. Accounts
CREATE TABLE IF NOT EXISTS Accounts (
    AccountID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ClientID BIGINT NOT NULL,
    AccountNumber VARCHAR(50),
    AccountStatus VARCHAR(20),
    FOREIGN KEY (ClientID) REFERENCES Clients(ClientID)
);

-- 7. Portfolios
CREATE TABLE IF NOT EXISTS Portfolios (
    PortfolioID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ClientID BIGINT NOT NULL,
    AccountNumber VARCHAR(50),
    PortfolioText VARCHAR(255),
    OnboardingDate DATE,
    OffboardingDate DATE,
    Status VARCHAR(20),
    FOREIGN KEY (ClientID) REFERENCES Clients(ClientID)
);

-- 8. QuestionnaireSections
CREATE TABLE IF NOT EXISTS QuestionnaireSections (
    SectionID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    SectionName VARCHAR(255) NOT NULL,
    DisplayOrder INT DEFAULT 0
);

-- 9. QuestionnaireQuestions
CREATE TABLE IF NOT EXISTS QuestionnaireQuestions (
    QuestionID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    SectionID BIGINT NOT NULL,
    QuestionText TEXT NOT NULL,
    QuestionType VARCHAR(20),
    IsMandatory BOOLEAN DEFAULT FALSE,
    Options TEXT,
    DisplayOrder INT DEFAULT 0,
    RiskFactorKey VARCHAR(50),
    FOREIGN KEY (SectionID) REFERENCES QuestionnaireSections(SectionID)
);

-- 10. CaseQuestionnaireResponses
CREATE TABLE IF NOT EXISTS CaseQuestionnaireResponses (
    ResponseID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CaseID BIGINT NOT NULL,
    QuestionID BIGINT NOT NULL,
    AnswerText TEXT,
    UNIQUE (CaseID, QuestionID),
    FOREIGN KEY (CaseID) REFERENCES Cases(CaseID),
    FOREIGN KEY (QuestionID) REFERENCES QuestionnaireQuestions(QuestionID)
);

-- 11. MaterialChangeConfigs
CREATE TABLE IF NOT EXISTS MaterialChangeConfigs (
    ConfigID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    EntityName VARCHAR(100),
    ColumnName VARCHAR(100),
    Category VARCHAR(20),
    UNIQUE (EntityName, ColumnName)
);

-- 12. MaterialChanges
CREATE TABLE IF NOT EXISTS MaterialChanges (
    ChangeID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ChangeDate TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    ClientID BIGINT,
    EntityID BIGINT,
    EntityName VARCHAR(255),
    ColumnName VARCHAR(255),
    OperationType VARCHAR(50),
    OldValue TEXT,
    NewValue TEXT,
    Status VARCHAR(20) DEFAULT 'PENDING',
    Category VARCHAR(20),
    FOREIGN KEY (ClientID) REFERENCES Clients(ClientID)
);
```

### SCREENING SERVICE - 6 Missing Tables

```sql
-- 1. BatchRuns
CREATE TABLE IF NOT EXISTS BatchRuns (
    BatchID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    BatchName VARCHAR(255) NOT NULL,
    RunStatus VARCHAR(50),
    NotificationStatus VARCHAR(50),
    FeedbackCount INT,
    MappingSnapshotID BIGINT,
    ClientCount INT,
    RunGroupId VARCHAR(64),
    CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UpdatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 2. BatchRunErrors
CREATE TABLE IF NOT EXISTS BatchRunErrors (
    ErrorID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    BatchID BIGINT,
    RecordID VARCHAR(100),
    ErrorCode VARCHAR(50),
    ErrorMessage TEXT,
    FOREIGN KEY (BatchID) REFERENCES BatchRuns(BatchID)
);

-- 3. MappingConfigs
CREATE TABLE IF NOT EXISTS MappingConfigs (
    MappingID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TargetPath VARCHAR(255) NOT NULL,
    SourceField VARCHAR(255),
    DefaultValue VARCHAR(255),
    Transformation VARCHAR(255)
);

-- 4. MappingConfigSnapshots
CREATE TABLE IF NOT EXISTS MappingConfigSnapshots (
    SnapshotID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    VersionLabel VARCHAR(100) NOT NULL,
    CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CreatedBy VARCHAR(50) DEFAULT 'SYSTEM',
    Source VARCHAR(20) DEFAULT 'MANUAL',
    ConfigJson TEXT NOT NULL
);

-- 5. BatchScreeningRuns
CREATE TABLE IF NOT EXISTS BatchScreeningRuns (
    Id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RunGroupId VARCHAR(64) NOT NULL UNIQUE,
    FileName VARCHAR(255),
    SystemId VARCHAR(100),
    TotalClientCount BIGINT,
    TotalBatches INT,
    BatchesCompleted INT DEFAULT 0,
    OverallStatus VARCHAR(20) DEFAULT 'DETECTED',
    PersistClients BOOLEAN DEFAULT FALSE,
    CallbackWebhookUrl VARCHAR(1000),
    ErrorMessage TEXT,
    CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CompletedAt TIMESTAMP
);

-- 6. BatchScreeningStaging
CREATE TABLE IF NOT EXISTS BatchScreeningStaging (
    Id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RunGroupId VARCHAR(64) NOT NULL,
    ClientId BIGINT,
    TitlePrefix VARCHAR(50),
    FirstName VARCHAR(255),
    MiddleName VARCHAR(255),
    LastName VARCHAR(255),
    TitleSuffix VARCHAR(50),
    Citizenship1 VARCHAR(100),
    Citizenship2 VARCHAR(100),
    OnboardingDate DATE,
    Status VARCHAR(20),
    NameAtBirth VARCHAR(255),
    NickName VARCHAR(255),
    Gender VARCHAR(10),
    DateOfBirth DATE,
    Language VARCHAR(50),
    Occupation VARCHAR(255),
    CountryOfTax VARCHAR(100),
    SourceOfFundsCountry VARCHAR(100),
    FatcaStatus VARCHAR(50),
    CrsStatus VARCHAR(50),
    AddressLine1 VARCHAR(500),
    City VARCHAR(255),
    ZipCode VARCHAR(50),
    Province VARCHAR(255),
    Country VARCHAR(100),
    Nationality VARCHAR(100),
    LegDocType VARCHAR(100),
    IdNumber VARCHAR(255),
    PlaceOfBirth VARCHAR(255),
    CityOfBirth VARCHAR(255),
    CountryOfBirth VARCHAR(100),
    ProcessingStatus VARCHAR(20) DEFAULT 'PENDING',
    CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### RISK SERVICE - 2 Missing Tables

```sql
-- 1. RiskMappings
CREATE TABLE IF NOT EXISTS RiskMappings (
    MappingID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TargetPath VARCHAR(255) NOT NULL,
    SourceField VARCHAR(100),
    DefaultValue VARCHAR(255),
    Category VARCHAR(100)
);

-- 2. BatchRuns (Risk Service)
CREATE TABLE IF NOT EXISTS BatchRuns (
    BatchID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    BatchName VARCHAR(255) NOT NULL,
    RunStatus VARCHAR(50),
    NotificationStatus VARCHAR(50),
    FeedbackCount INT,
    CreatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UpdatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## Manual Draw.io Updates

If you prefer to update the diagram manually:

### For Each Missing Table:

1. **Open** `/Users/venusamineni/Documents/KYC_ER.drawio` in draw.io
2. **Locate** the service swimlane where the table belongs (Viewer, Screening, or Risk)
3. **Create New Table**:
   - Insert → Table
   - Set dimensions and position
   - Add field rows from the SQL above
4. **Format**:
   - Primary Keys: Add 🔑 prefix
   - Foreign Keys: Add 🔗 prefix
   - Colors: Match the swimlane (Green for Viewer, Blue for Screening, Orange for Risk)

### Reference Colors:
- **Viewer Service**: `#d5e8d4` (light green)
- **Screening Service**: `#dae8fc` (light blue)
- **Risk Service**: `#ffe6cc` (light orange)

---

## Recommended Approach

**Option 1: Use SQL Scripts** (FASTEST)
- Copy the SQL definitions above
- Add to your service schema.sql files
- Database schema is updated immediately
- Don't worry about the diagram for now

**Option 2: Update Diagram Manually** (BEST FOR DOCS)
- Open the diagram in draw.io
- Add tables one-by-one to avoid stack overflow
- Save frequently
- Keep a working backup

**Option 3: Create New Diagram** (CLEANEST)
- Start fresh draw.io diagram
- Add all tables at once
- More organized structure
- Can reference old diagram

---

## Files Available

- **Original Diagram** (Working): `/Users/venusamineni/Documents/KYC_ER.drawio`
- **Backup Copy**: `/Users/venusamineni/Documents/KYC_ER.drawio.backup`
- **Audit Report**: `/Users/venusamineni/Downloads/ER_DIAGRAM_AUDIT.md`
- **SQL Scripts**: See above in this guide

---

## Next Steps

1. **Immediate**: Use the SQL scripts to update your database schemas
2. **Soon**: Update the diagram by adding tables manually one service at a time
3. **Later**: Create a cleaner, new diagram with all tables included

Let me know if you need help with any of these approaches!
