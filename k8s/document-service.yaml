apiVersion: apps/v1
kind: Deployment
metadata:
  name: document-service
  namespace: kyc
  labels:
    app: document-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: document-service
  template:
    metadata:
      labels:
        app: document-service
    spec:
      containers:
        - name: document-service
          image: REGION-docker.pkg.dev/PROJECT_ID/kyc-ms/document-service:IMAGE_TAG
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: kyc-config
            - secretRef:
                name: kyc-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 30
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: kyc
spec:
  selector:
    app: document-service
  ports:
    - port: 8085
      targetPort: 8085
  type: ClusterIP
