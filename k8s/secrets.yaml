# ---------------------------------------------------------------
# TEMPLATE — do NOT commit real values to source control.
# Populate values via:
#   kubectl create secret generic kyc-secrets --from-env-file=.env.gcp -n kyc
# or use GCP Secret Manager with the Workload Identity CSI driver.
# ---------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: kyc-secrets
  namespace: kyc
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_WITH_STRONG_SECRET"
  DB_URL:      "jdbc:postgresql://CLOUD_SQL_IP:5432/kycdb"
  DB_USER:     "REPLACE_WITH_DB_USER"
  DB_PASSWORD: "REPLACE_WITH_DB_PASSWORD"
  SFTP_USER:     "REPLACE_WITH_SFTP_USER"
  SFTP_PASSWORD: "REPLACE_WITH_SFTP_PASSWORD"
  GPG_PASSPHRASE: "REPLACE_WITH_GPG_PASSPHRASE"
